import { closeFundingOrder, createFundingOrder, mostProfitableSymbol } from './main.utils.mjs'

const main = async () => {
  // const profitable = await mostProfitableSymbol()
  // const apt = profitable.find((x) => x.baseCurrency === 'APT')
  // if (apt === undefined) throw new Error('No APT found')
  // console.log(profitable)
  // const orders = await createFundingOrder(apt.baseCurrency, apt.longMatch!, apt.shortMatch!, 500)
  // console.log(orders)
  // create close orders
  // await closeFundingOrder('APT', '49d60a607448adb8c93ecde3ec8ec1a6', '969322781155667969')
}

void main()
